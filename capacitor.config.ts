import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.halolscan.app',
  appName: 'Halol Sertifikat Skanner',
  webDir: 'dist'
};

export default config;
